# Include headers.
-i

# Silent -- no output, progress bar, or errors.
-s

# Actually, do show errors.
-S

# Actually, do show interesting output. In fact, be verbose. Just don't show the
# damn progress bar.
-v

--header "Accept: application/json"

# Simulate IE 9, Windows 7.
# user-agent = "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"

# Chrome 45, OS X.
# user-agent = "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_10_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/45.0.2454.85 Safari/537.36"

# See: http://stackoverflow.com/a/23410059/599258
#referer = ";auto"

# read .netrc for user name and password OR URL
#netrc-optional

#--netrc

# pick the most secure methods of authentication that the server accepts
# anyauth

# !!! remote-time dosn't work with ftp upload from stdin !!! : use -R switch for downloading
#remote-time
#remote-name-all
#create-dirs

# Don't show/download error document - only error code
#fail

#verbose
#silent
#show-error

# FTP setup
#ftp-create-dirs
#ftp-ssl
#ftp-pasv
#ftp-method = nocwd

#proxy = xxx.xxx.xxx.xxx:80
